doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style.
            body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
            .container { max-width: 600px; margin: 0 auto; padding: 20px; }
            .header { background-color: #f8f9fa; padding: 15px; text-align: center; }
            .content { padding: 20px 0; }
            .footer { text-align: center; font-size: 12px; color: #666; }
            .highlight { color: #dc3545; }
            table { width: 100%; border-collapse: collapse; margin: 15px 0; }
            th, td { padding: 8px; border: 1px solid #ddd; }
            th { background-color: #f8f9fa; }
            a { color: #0000FF; text-decoration: underline; }

    body
        .container
            .header
                h2 Upcoming AMC Payment Alert

            .content
                p Dear Team,

                p This is to inform you about an upcoming AMC payment for:

                table
                    tr
                        th Customer Name
                        td #{client}
                    tr
                        th Product
                        td #{product}

                h3 AMC Details:
                table
                    thead
                        tr
                            th AMC Cycle
                            th Upcoming Days
                            th Due Amount
                            th Due Date
                    tbody
                        tr
                            td= amc.cycle || 'N/A'
                            td #{amc.upcoming} days
                            td.highlight= amc.amount ? `₹${amc.amount}` : 'N/A'
                            td.highlight= amc.dueDate || 'N/A'

                p Please make the payment before the due date to avoid any service disruption.
                    a(href=amc.link) View AMC Details
                p If the payment is already made, please update the payment status using the link below:
                    a(href=amc.updateLink) Update Payment Status

                h3 Contact Person Details:
                table
                    thead
                        tr
                            th Name
                            th Designation
                            th Email
                            th Phone
                    tbody
                        each contact in contacts
                            tr
                                td #{contact.name}
                                td #{contact.designation}
                                td #{contact.email}
                                td #{contact.phone}

            .footer
                p This is an automated reminder. Please do not reply to this email.
                p If AMC payment is already made, please update the payment status using the Update link above.
                p If you have any questions, please contact the system administrator.
